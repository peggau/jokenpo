class machineOptions {
    constructor({opt}) {
        this._opt = opt
    }

    sort(){
        const sortOpt = Math.floor() * 3 - 0
        return sortOpt
    }

    set opt(option) {
        this._opt = option
    }

    get opt() {
        return this._opt
    }

}

module.exports = machineOptions
